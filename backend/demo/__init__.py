"""In-app demo controls: replayable proof scenarios and synthetic traffic.

The frozen scenarios under ``fixtures/scenarios`` are the project's evidence
that the decision path behaves as claimed. Exposing them over HTTP is what lets
a reviewer watch that evidence being produced instead of taking a shell script's
word for it.
"""
